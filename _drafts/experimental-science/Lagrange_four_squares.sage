'''
In 1770 Lagrange showed that every natural number can be written as the sum of four squares.  There are many proofs for this fact now.  We examine algorithmic approaches to compute the decomposition
'''

def prime_to_squares(p):
    if p == 2:
        return (1,1)
    elif mod(p,4) == 
